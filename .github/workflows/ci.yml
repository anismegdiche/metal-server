name: CI Pipeline

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  enable-deploy:
    # set to true to enable deployment
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI enabled"

  matrix-test-and-build:
    if: github.ref_name == 'dev'
    strategy:
      matrix:
        os:
          - ubuntu-22.04
          - ubuntu-24.04
          - ubuntu-26.04

          - windows-2022
          - windows-2025

          - macos-14
          - macos-15
          - macos-26

        node:
          - "24.16.0"
          # - "24.17.0"
          # - "24.18.1"
          # - "24.19.0"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        env:
          NODE_NO_WARNINGS: 1
        with:
          ref: dev

      - name: Set up Node.js
        uses: actions/setup-node@v3
        env:
          NODE_NO_WARNINGS: 1
        with:
          node-version: ${{ matrix.node }}

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Run tests
        run: yarn workspace @metal/server test

      - name: Compile
        run: |
          yarn workspace @metal/server server:build
          yarn workspace @metal/studio studio:build

  docker-image-test-and-build:
    if: github.ref_name == 'dev'

    runs-on: ubuntu-latest

    permissions:
      contents: read # Reduced permissions since we're not pushing

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and test image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: .docker/metal-test/dockerfile
          tags: metal-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true # Makes the image available locally
          push: false # Explicitly disable pushing

      - name: Run tests in container
        run: |
          docker run --rm \
            metal-test:latest \
            yarn vitest run --project @metal/server

  sync-docs:
    needs:
      - enable-deploy
      - matrix-test-and-build
      - docker-image-test-and-build
    if: github.ref_name == 'dev' && github.actor != 'nektos/act' && needs.docker-image-test-and-build.result == 'success' && needs.matrix-test-and-build.result == 'success'

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        env:
          NODE_NO_WARNINGS: 1
        with:
          ref: dev

      - name: Run GitHub File Sync
        uses: BetaHuhn/repo-file-sync-action@v1
        with:
          GH_PAT: ${{ secrets.GH_PAT }}
          SKIP_PR: true

  deploy:
    needs:
      - enable-deploy
      - matrix-test-and-build
      - docker-image-test-and-build
    if: github.ref_name == 'dev' && github.actor != 'nektos/act' && needs.docker-image-test-and-build.result == 'success' && needs.matrix-test-and-build.result == 'success'

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        env:
          NODE_NO_WARNINGS: 1
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          ref: dev

      - name: Force sync dev to latest (delete orphans)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git fetch origin
          git checkout dev
          git remote set-url origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/anismegdiche/metal-server.git
          git push origin dev:latest --force
