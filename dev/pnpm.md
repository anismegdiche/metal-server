**Key pnpm commands moving forward:**
- pnpm install — install all dependencies
- pnpm build — build root project
- pnpm -r build — build all workspaces (replaces npm run build --workspaces)
- pnpm add lodash — add dependency
- pnpm add -D typescript — add dev dependency
- pnpm test — run tests
- pnpm lint — run biome lint